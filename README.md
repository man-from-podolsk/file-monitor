# File Monitor

Скрипт для мониторинга целостности критических системных файлов через подсистему `auditd` ядра Linux.

* Отслеживает изменения содержимого и метаданных (права, владелец)
* Логирует различия (`diff`) при изменении текстовых файлов
* Поддерживает рекурсивный мониторинг каталогов и wildcard-шаблоны
* Работает на **Debian, Ubuntu, RHEL, CentOS, AlmaLinux, Rocky Linux, SLES, openSUSE**

---
## 🧠 Как работает file-monitor.service
Служба file-monitor - это демон на Bash, предназначенный для контроля целостности критических системных файлов с использованием подсистемы Linux Audit (auditd). Он не заменяет auditd, а интеллектуально дополняет его, делая события понятными и полезными.

### 🔁 Общий цикл работы

#### Загрузка конфигурации
- Скрипт читает **`/etc/file-monitor.conf`** (если не найден — использует встроенные значения по умолчанию).
- Поддерживаются: интервал проверки, пути для мониторинга, включение `diff` и др. [Ознакомиться чуть ниже](https://github.com/man-from-podolsk/file-monitor?tab=readme-ov-file#-%D0%BF%D0%B5%D1%80%D0%B5%D0%B7%D0%B0%D0%B3%D1%80%D1%83%D0%B7%D0%BA%D0%B0-%D0%BA%D0%BE%D0%BD%D1%84%D0%B8%D0%B3%D1%83%D1%80%D0%B0%D1%86%D0%B8%D0%B8)

#### Регистрация правил в auditd
- Для каждого указанного файла или каталога скрипт:
  - Рекурсивно находит все файлы
  - Исключает временные файлы и артефакты: `.swp`, `.tmp`, `.bkp`, `.bak`, `.old`, `.rpmnew`, `.rpmsave`, `~`, `README`, `readme`.
  - Формирует правила вида:

    ```ini
    -a always,exit -F path=/etc/fstab -F perm=wa -k file-monitor
    ```

  - Записывает их в **`/etc/audit/rules.d/file-monitor.rules`**
  - Применяет правила через **`augenrules --load`**

#### Инициализация кэша
- Для каждого отслеживаемого файла создаётся кэшированная копия в /var/cache/file-monitor/ (с сохранением метаданных).
Это позволяет:
  - Сравнивать текущее состояние с предыдущим
  - Строить diff при изменении
  - Обнаруживать изменения, произошедшие до запуска службы.
#### Основной цикл
- Каждые N секунд (по умолчанию — 60):
  - Запрашивает у ausearch события за последний интервал с ключом file-monitor
  - Фильтрует ложные срабатывания (success=no, name=(null), временные файлы),
- Для каждого изменённого файла:
  - Проверяет изменение метаданных (права, владелец, группа)
  - Проверяет изменение содержимого через **sha256sum**
  - Если контрольная сумма кешированной копии и наблюдаемого файла не совпадают - строит diff, если включено *enable_diff* = **true** и файл не слишком большой *max_file_size_kb* **>** размер проверяемого файла
  - Записывает всё в лог: /var/log/file-monitor/file-monitor.log

## Поддержка hot-reload
При получении сигнала SIGHUP (systemctl reload file-monitor) скрипт:
- перечитывает конфигурацию,
- обновляет правила auditd,
- перестраивает кэш,
- продолжает работу без остановки.
### Пример 
Вы добавили в конфигурационный файл ещё один путь для отслеживания, например /etc/file. После получения SIGHUB в логе будет следующее
```
####################################################################################################
19 Dec 2025 20:23:44 - Received SIGHUP: reloading configuration...
####################################################################################################

####################################################################################################
19 Dec 2025 20:23:44 - File '/etc/file' was cached by user 'system'. Details:
####################################################################################################
Initial cache created

Configuration reloaded successfully.
```
С этой опцией можно [ознакомиться чуть ниже](https://github.com/man-from-podolsk/file-monitor/new/main?filename=README.md#-%D0%BF%D0%B5%D1%80%D0%B5%D0%B7%D0%B0%D0%B3%D1%80%D1%83%D0%B7%D0%BA%D0%B0-%D0%BA%D0%BE%D0%BD%D1%84%D0%B8%D0%B3%D1%83%D1%80%D0%B0%D1%86%D0%B8%D0%B8)

## 🛡️ Что отслеживается
| Тип изменения | Пример | Логируется |
| --- | --- | --- |
| Изменение содержимого | echo "test" >> /etc/fstab | ✅ + diff |
| Смена владельца | chown nobody /etc/fstab | ✅ |
| Смена прав | chmod 600 /etc/fstab | ✅ |
| Удаление файла | rm /etc/fstab | ❗ пока не реализовано |
| Создание нового файла в каталоге | touch /etc/sudoers.d/new | ❗ (если каталог в path=) |
> Каталоги отслеживаются рекурсивно, но только существующие файлы на момент запуска/перезагрузки. Новые файлы в каталоге будут обнаружены после reload/restart.

## 📦 Безопасность и надёжность
- Работает только от root (требуется доступ к auditd).
- Не зависит от внешних бинарников, кроме auditd, bash 4+, coreutils.
- Валидация конфига: некорректные значения — ошибка, а не падение.
- Атомарное обновление кэша: используется временный файл + mv.
- Лог ротируется: через logrotate (10 МБ, 10 копий).
## 📝 Пример записи в логе

После выполнения следующих команд
```bash
echo "# test" >> /etc/fstab
chown nobody /etc/fstab
chmod 600 /etc/fstab
```
В файле лога будет следующая запись
```
####################################################################################################
19 Dec 2025 20:06:43 - File '/etc/fstab' was modified by user 'man-from-podolsk'. Details:
####################################################################################################
Metadata changed:
permissions: -rw-r--r-- → -rw-------
owner: root → nobody
Added lines:
18 | # test

```

## ⚙️ Требования
- Linux с ядром, поддерживающим audit
- Установленный auditd (audit в RHEL/SUSE)
- augenrules (есть в audit пакете на RHEL 8+, CentOS Stream, Alma, Rocky, Debian 10+)
- Bash 4+

Этот подход делает file-monitor простым, прозрачным и надёжным инструментом для аудита файлов — без сложных агентов, баз данных или внешних зависимостей.

## 📥 Установка

### Вариант 1: Универсальный установщик, при наличии интернета.
Скрипт:

* автоматически определит дистрибутив,
* установит зависимости (auditd / audit),
* загрузит актуальные файлы из репозитория,
* настроит службу и запустит её.

```bash
curl -fsSL https://raw.githubusercontent.com/man-from-podolsk/file-monitor/main/git_install.sh | sudo bash
```

### Вариант 2: Универсальный установщик, локальная версия.

Подходит для изолированных систем:

**1.** Скачайте архив с релизом или клонируйте репозиторий на машину удобным способом (USB, SCP и т.д.).

**2.** Распакуйте и перейдите в директорию

```bash
tar xzf file-monitor.tar.gz
cd file-monitor
```

**3.** Сделайте скрипт исполняемым

```bash
chmod +x local_install.sh
```

**4.** Запустите локальную установку

```bash
sudo ./local_install.sh
```

### Вариант 3: Установка из исходников

```bash
sudo install -Dm755 script/file-monitor.sh /usr/local/bin/file-monitor.sh
sudo install -Dm644 systemd/file-monitor.service /etc/systemd/system/file-monitor.service
sudo install -Dm644 config/file-monitor.conf /etc/file-monitor.conf
sudo install -Dm644 logrotate/file-monitor /etc/logrotate.d/file-monitor
sudo mkdir -p /var/cache/file-monitor /var/log/file-monitor
sudo systemctl daemon-reload
sudo systemctl enable --now file-monitor
```

### Вариант 4: Установка из пакета

Готовые пакеты доступны в разделе [Releases.](https://github.com/man-from-podolsk/file-monitor/releases)

## ⚙️ Настройка

Основной конфигурационный файл:
**/etc/file-monitor.conf**

Поддерживаемые параметры:

| Параметр | Описание |
|---|---|
| interval | Интервал проверки событий в секундах |
| audit_key | Ключ, используемый в правилах auditd |
| enable_diff | true/false — включить сравнение содержимого |
| max_file_size_kb | Макс. размер файла для генерации diff (КБ) 0 - нет ограничений, но выше нагрузка |
| path | Путь к файлу или каталогу (можно указывать несколько раз) |
| **Лучше не** | **менять параметры ниже** |
| cache_dir | Каталог для кэширования копий файлов |
| log_file | Путь к лог-файлу |

## 🗑️ Удаление
Чтобы полностью удалить file-monitor, используйте скрипт удаления:

```bash
curl -fsSL https://raw.githubusercontent.com/man-from-podolsk/file-monitor/main/uninstall.sh | sudo bash
```

Если файлы доступны локально:

```bash
chmod +x ./uninstall.sh
./uninstall.sh
```

### Скрипт:

- остановит и отключит службу,
- удалит все конфигурационные файлы,
- очистит правила auditd,
- удалит логи и кэш.

### Если вы устанавливали службу из пакета deb или rpm - воспользуйтесь пакетным менеджером.

Можно использовать ключ --purge для полного удаления (конфигурация, кэш, логи)

## 🔄 Перезагрузка конфигурации

После изменения /etc/file-monitor.conf выполните:

```bash
sudo systemctl reload file-monitor
```

Или

```bash
sudo kill -HUP $(systemctl show -p MainPID --value file-monitor)
```

Служба перечитает конфигурацию без остановки мониторинга.

**!Важно обновлённая конфигурация применится после истечения текущего $interval.**

